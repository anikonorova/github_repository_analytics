models:
  - name: int_github__contributors_spine
    description: Contributor identity spine built from commits, pull requests, issues, and reviews. Deduplicates contributors across event sources to create a consistent contributor key.
    columns:
      - name: contributor_key
        tests:
            - not_null
            - unique